<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Error - Vanshika Verma Portfolio</title>
    <!-- Bootstrap for consistent styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <h1 class="display-4">Oops! Something Went Wrong</h1>
                <p class="lead">Error: {{message}}</p>  <!-- Use {{message}} from locals for consistency -->
                {{#if error.status}}
                    <p><strong>Status Code:</strong> {{error.status}}</p>
                {{/if}}
                <hr class="my-4">
                <p>Return to <a href="/">Home</a> or try refreshing the page.</p>
                {{#if (eq env 'development')}}  <!-- Now works with the registered helper -->
                    <details class="mt-3">
                        <summary>Development Details (Click to Expand)</summary>
                        <pre>{{json error}}</pre>  <!-- Uses the optional json helper for stack trace -->
                    </details>
                {{/if}}
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>